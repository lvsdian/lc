26 27 283 80 15	18		double pointer
215 75 88 				extension of merge sort & quick sort (215可以用PriorityQueue)
11 125 167 				collision(碰撞) pointer
344 345 				'^' & double pointer
209 3 438 76 			sliding window(滑动窗口，438 76用数组计数字符)
1 290 205 454 447 		hashmap
219 217 220				hashmap
349  					set/stream
350						hashmap  double pointer
242						hashmap	数组计数字符
202						hashmap/set
451						hashmap、PriorityQueue
49						hashmap(字母异位词问题可以用数组计数字符来解决，也可以转成char数组再排序，排完序直接比较)
220						treeSet
206					反转链表
92					反转一个链表的一部分
86					将一个链表分成分成两个部分(<target在前，>=target在后)
328					将奇索引位节点排在前，偶索引位节点排在后，解法类似86
2 445				两个链表的val相加
203					链表中删除指定元素
83，82				删除排序链表中的重复元素，快慢指针
21					合并两个链表
24					链表两两节点交换(如果要操作哑结点，创建两个哑结点，一个用于操作，一个用于返回)
147					对链表插入排序(如果要操作哑结点，创建两个哑结点，一个用于操作，一个用于返回)
148					对链表归并排序(自顶向下)，快慢指针
237					改变节点值来删除节点
19					删除链表末尾第n个元素，双指针
61					环形链表解决链表旋转问题
143,234				快慢指针，翻转链表
遍历map：
	map.forEach((k,j)->{
		System.out.println(k+""+j);
	});
